pipeline {
    agent { label 'agent1' }  

    tools {
        maven "M3"
    }
    environment {
        NEXUS_URL = "${NEXUS_URL}" 
        DOCKER_USERNAME = "${DOCKER_USERNAME}"
        DOCKER_PASSWORD = "${DOCKER_PASSWORD}"
        GITHUB_CREDENTIAL_ID = "${GITHUB_CREDENTIAL_ID}"
        VERSION = "${params.VERSION}"
        DEV_VERSION = "${params.DEV_VERSION}"
        
    }
    stages {
        stage("Release and push to Nexus") {
            steps {
                withCredentials([usernamePassword(credentialsId: env.GITHUB_CREDENTIAL_ID, passwordVariable: 'pass', usernameVariable: 'user')]) {
                    sh "mvn release:clean"
                    sh "mvn release:prepare -Darguments=\"-Dmaven.test.skip=true\" -Dtag=${VERSION} -DreleaseVersion=${VERSION} -DdevelopmentVersion=${DEV_VERSION} -DpushChanges=false -Dusername=$user -Dpassword=$pass"
                    sh "mvn release:perform -Darguments=\"-Dmaven.test.skip=true -Dmaven.javadoc.skip=true\""
                    sh "git push origin ${DEV_VERSION}"
                }
            }
        }
    }
    post {
        success {
            slackSend color: "good", message:"Build successful - ${env.JOB_NAME} ${env.BUILD_NUMBER} (<${env.BUILD_URL}|Open>)"
        }
        failure {
            slackSend color: "danger", message:"Build failed  - ${env.JOB_NAME} ${env.BUILD_NUMBER} (<${env.BUILD_URL}|Open>)"
        }
    }
}
